<div>
  <%= flash[:notice] %>
</div>


<h1>Editing Book</h1>

<%= form_with model: @book do |f| %>

  Title</br>
  <%= f.text_field :title %></br>
  
  <p>Body</br>
  <%= f.text_area :body %></br>
  
  <%= f.submit 'Update Book' %></br>
  
  <%= link_to "Show", book_path(@book.id) %>
  <a href ="/books">Back</a>
<% end %>
